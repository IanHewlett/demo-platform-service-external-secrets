---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-admin
spec:
  provider:
    vault:
      server: <path:test/data/svc/vault#vault_address>
      path: secret
      version: v2
      auth:
        kubernetes:
          mountPath: "local-minikube-us-east-0"
          role: "di-admin-kubernetes-role"
      namespace: admin
